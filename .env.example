# app：第一次打开会复制到 ~/Library/Application Support/chat-jev/.env，在那里填（app 读不到 shell 里 export 的变量）。
# 从源码跑：复制成项目根目录的 .env 后填好，也可以直接 export 到环境变量。

# 后端预设：vercel（默认，TypeSafe 兼容端点）| vercel-native（/v1/evaluate）| typesafe（直连）
JEV_BACKEND=vercel

# 密钥。没设 JEV_API_KEY 时会依次找 AI_GATEWAY_API_KEY / TYPESAFE_API_KEY
# JEV_API_KEY=
AI_GATEWAY_API_KEY=

# 想指定别的地址 / 模型时再打开（默认由 JEV_BACKEND 决定）
# JEV_BASE_URL=https://ai-gateway.vercel.sh/typesafe
# JEV_MODEL=typesafe-ai/jev

# 判别参数
JEV_THRESHOLD=0.5        # 表里一致概率 >= 阈值 → yes
JEV_RELATION=女朋友       # 对方和你的关系，会写进给模型的背景里
JEV_HISTORY=12           # 每次判别带多少条上下文
JEV_TIMEOUT=15

# ---- 文本 LLM（可选）：生成「接下来该怎么做」的候选，再由 Jev 打概率分布 ----
# 留空 = 不启用；填了就自动启用，`--no-actions` 关掉。
# 默认实现走 Anthropic 官方 SDK（chat_jev/llm.py 的 LLMClient.complete，换 provider 只改那一处）。
LLM_API_KEY=
# 直连 Anthropic 留空；走 Vercel AI Gateway 填 https://ai-gateway.vercel.sh（模型名要写 anthropic/claude-opus-5）
LLM_BASE_URL=
LLM_MODEL=
LLM_TIMEOUT=30

# ---- app 专用：双击打开时相当于 `chat-jev watch --app qq` 再加上这里的参数 ----
# JEV_WATCH_ARGS="--pick-only --interval 0.5"
